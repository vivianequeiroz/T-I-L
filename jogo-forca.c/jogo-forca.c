#include <stdio.h>
#include <stdlib.h>

int main() {

    char secretWord[20];

    sprint(secretWord, "MELANCIA");
     
     int hit = 0;
     int hanged = 0;

     do {
         
     } while (!acertou == 0 && enforcou == 0);



}